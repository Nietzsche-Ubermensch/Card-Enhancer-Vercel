name: build (3.8)
on:
  push:
  pull_request:
  repository_dispatch:
    types: [vercel.deployment.ready, vercel.deployment.success]
jobs:
  build:
    name: build (3.8)
    runs-on: ubuntu-latest
    steps:
      - name: nitro host
        run: echo "JUG-19 Card Enhancer is TanStack Start + Nitro. Python 3.8 matrix is N/A."
