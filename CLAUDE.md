# Card Enhancer — Claude Code / Grok compat

Always use TypeScript. Prefer functional components.
Skills live in `.grok/skills/` and are linked from `.claude/skills/` (do not fork).
Platform skills (`auth`, `neon`, `xai-api`, `design-ui`, `og`, `imagine`) stay Grok-only.
