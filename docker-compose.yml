version: '3.9'

services:
  card-enhancer:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./outputs:/app/outputs
      - ./models:/app/models
      - ./jobs.db:/app/jobs.db
    env_file:
      - .env
    restart: unless-stopped
